language: node_js
node_js:
  - 14
services:
  - postgresql
addons:
  postgresql: "12"
  apt:
    packages:
      - postgresql-12
      - postgresql-client-12
env:
  global:
    - PGPORT=5432
before_script:
  - psql -c 'create database tests;' -U postgres
  - psql -c "CREATE USER docker WITH PASSWORD '123mudar';" -U postgres
script:
  - eslint 'src/**'
  - yarn test:coveralls
